# TICKET — BN42: the four WBPs and the FE map, built by script at the referee's boxes

> STATUS: open — cut 1 Sep 2026 by the cloud lead. OWNER: **terminal**, LIVE EDITOR.
> DEPENDS ON BN41 rung 1. requires: editor-live, unreal-mcp (`list_toolsets` first, the
> four failure rules apply). **Law 7: widgets land by committed Tools/bn/bn41_*.py
> scripts in the bn11 pattern (bn11_lib is the transport), never hand-placed.**
> Layout truth: `01-MENU-MEASURED.md` — if a number is not in the referee, re-read the
> node, do not eyeball it. The HUD was built this way and the founder called it perfect;
> same bar.

## Build (in order)

1. **`WBP_BNPanelBorder`** — the notch language (§1): rounded rect + two 88×4 edge bars +
   3px inset. Procedural (Border/Image brushes), no texture. Reused by everything below.
2. **`WBP_BNMenuListButton`** — 311×28 row, pitch 40 in lists; amber caret 3×65 on the
   focused row; label left, optional value right (`BNUIColors` tokens, no hex in assets).
   Parent class `UButton`-based per the pause screen's rows — check how bn11 did the
   pause rows first and match it.
3. **`WBP_BNScreen_FrontEnd`** (parent `UBNScreen_FrontEnd`) — §2 boxes: Menu Combo at
   (69,138) 349×510 — news card 349×222 (our art or flat panel), Menu in Border 349×186
   with rows PLAY / two disabled slots / QUIT, Description Frame 349×37 at y473; Party
   List chassis at (862,397); Profile Bar (0,670 1280×50). BindWidget names MUST match
   the C++: `PlayButton`, `QuitButton`, `DescriptionText`.
4. **`WBP_BNScreen_PlaySetup`** (parent `UBNScreen_PlaySetup`) — §4 CG_Lobby: Menu Combo
   (69,76) with Preview Photo 349×196.7 + rows MAP/MODE/BOTS/START + description; Game
   Settings Breakdown chassis at (466,76) 349×332 mirroring the values (bind the THREE
   value texts: `MapValueText`, `ModeValueText`, `BotsValueText` — they live in the rail
   rows; the breakdown may reuse the same texts or ship M2). `StartButton` is the Action
   Button row. Optional: `BackButton`, `DescriptionText`.
5. **`FE_MainMenu.umap`** at `/Game/Maps/FE_MainMenu` — WorldSettings GameModeOverride =
   `BNFrontEndGameMode`; content = a backdrop (BR_Spillway vista camera or flat) — the
   menu is UI, the map is a stage. Generated by script, law 7.
6. Canvas is 1280×720 design-space ×1.5 → authoring at 1920×1080 with DPI rule matching
   the HUD WBPs (read WBP_BNHUD's setup FIRST and copy its approach exactly).

## Prove (the loop, not the look)

- [ ] PIE on FE_MainMenu: menu appears, mouse works, focus on PLAY
- [ ] PLAY → setup; Escape pops back; values cycle and print correctly
- [ ] START on Spillway/FFA/8 → match with YOU + 7 bots (scoreboard count = 8)
- [ ] START with TDM → two teams, 4v4, team colours in feeds
- [ ] Pause → LEAVE MATCH → back on the menu map with the menu up
- [ ] Repeat loop twice — the second lap catches the stale-subsystem class of bug
- [ ] Screenshots of both screens beside the Figma frames → founder

## Done when
- [ ] All five assets landed by committed scripts; script paths recorded here
- [ ] The loop walks, twice, observations here
- [ ] Deviations from the referee listed with node ids (severity: blocks / cosmetic)

## Log
