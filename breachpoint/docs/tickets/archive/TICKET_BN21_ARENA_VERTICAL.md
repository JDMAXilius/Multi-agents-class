# TICKET — BN21: the arena's stairs are solid boxes; the map is one-way downward

> STATUS: **FIXED and CLOSED 28 Aug 2026 — archived.** Founder took option 1. The two
> solid stair volumes are gone; **26 walkable treads** stand in their place, generated by
> the committed script, and bots climb them: pawns caught mid-flight **1 → 16** across 90
> PIE samples, footprint hits **5 → 53**. The map is no longer one-way downward.
> Original: open — cut 26 Aug 2026 by mac terminal, from the AIB9 traversal measurement.
> Needs a founder ruling before any build: this is LEVEL GEOMETRY, not a bot fix.

## What the manifest intends

`Content/Data/arena_manifest.json`, verbatim:

- `stair_west` x[13,16] y[19,21] z[0,4] — *"ground-to-mid stair volume"*
- `stair_east` x[24,27] y[19,21] z[0,4] — the mirror
- **The Gantry**: *"nothing walkable continues to height 8 — the Gantry above is
  grapple-only by design"*

So ground↔mid is MEANT to be walkable, and mid→upper is grapple-only on purpose.

## What is actually built

`arena_plan.py` emits `landmark_stair` as a single AABB, and `build_arena.py`'s
`spawn_box` spawns **one scaled cube**. `stair_west` is therefore a solid
3m x 2m x **4m block** — a 400uu wall.

```
character MaxStepHeight :  45uu
character jump apex     :  90uu   (JumpZVelocity 420, gravity 980)
stair rise              : 400uu   -> unclimbable by a factor of 4.4
```

The manifest's own `doubts` list already suspected it: *"real ramp geometry may occlude
them"*.

## The consequence, measured

There is **no walkable route from ground to mid** for anyone — bot or player. Mid→upper is
grapple-only and the grappleshot is **not implemented** (one mention in `BNCollision.h`;
no ability, no input tag, no AI verb). So the whole map is one-way downward: SP7/SP8 spawn
on the Gantry, bodies drop off, and nothing returns.

AIB9's instrument, one match: 80,237 move refusals, 97% with the goal BELOW the bot. The
drop half is fixed (JumpMaxDepth 800 -> 1000, refusals/switch 9.54 -> 0.42). The climb half
cannot be fixed by any navlink — a link promising a 400uu climb against a 90uu apex is a
promise the body cannot keep, and the bot would launch and fall. That is the same class of
breach this project refused when raising JumpLength.

## The options (founder's call — do NOT pick one unilaterally)

1. **Make the stair volumes real steps.** Nine steps at <=45uu rise, 33uu tread inside the
   existing 3m run — walkable with no jump at all, navmesh covers it natively, and it
   restores ground<->mid for bots AND players. Smallest change; honours the manifest's
   stated intent.
2. **Implement the grappleshot.** The map's designed vertical answer, and the only thing
   that makes the Gantry reachable as authored. Much larger.
3. **Accept one-way down** and stop spawning anyone on the Gantry (drop SP7/SP8), so the
   upper tier becomes a drop-in vantage rather than a spawn that strands.

Option 1 and option 3 are compatible and both are small. Option 2 is a feature.

## Kickoff

- requires: engine-installed, editor-live (the rebuild), and a founder ruling first
- owner_path: `Tools/blockout/arena_plan.py`, `Content/Maps/BR_Arena01.umap` (BINARY —
  law 7: one owner, lock before editing), this ticket

## Done when

- [x] Founder ruling recorded here — **OPTION 1**, delivered through BN22's takeover
      directive ("traversal/jumping between platforms back")
- [x] Steps emitted by the committed script, never hand-placed (law 7) —
      `Tools/blockout/bn21_stairs_mcp.py`, 26 treads; the level SAVE was one hand action
      against a live editor (World Partition OFPA, no MCP tool for it), recorded in
      BN22 §3 as the terminal's single manual step
- [x] Ground<->mid traversal proven live: bots ascend — mid-flight pawns **1 → 16**,
      footprint hits **5 → 53**, across **90 PIE samples**
- [ ] ~~The AIB9 climb-band failures (dz +300..+499, 501 in the sample) fall to ~0~~ —
      **REASSIGNED to AIB9**, which owns the move-refusal instrument and is open. Not
      re-measured here; do not read the boxes above as having measured it

## Log

### 2026-08-28 — closing entry. Option 1 built, proven, archived.

**What was built:** the two solid `stair_west`/`stair_east` volumes — 400uu walls against
a 45uu step and a 90uu apex, the 4.4x this ticket opened with — are replaced by **26
walkable treads**, emitted by `Tools/blockout/bn21_stairs_mcp.py`. Nine steps was this
ticket's arithmetic; 26 is what the navmesh actually wanted, which is the whole lesson
below. The Gantry stays grapple-only exactly as the manifest authored it — that half was
never the defect, and BN23 (humans) and AIB19 (bots) now supply the grapple.

**Measured, live:** pawns caught mid-flight **1 → 16** across **90 PIE samples**;
footprint hits **5 → 53**. Bots climb.

**THE LESSON, and the reason the first attempt failed — worth more than the fix.** The
treads had to be built for the **NAVMESH AGENT** (`AgentMaxStepHeight 35`,
`AgentRadius 34`), not for the character's `MaxStepHeight 45`. The first attempt sized
risers to 45 — correct against this ticket's own arithmetic in the "options" section
above, which reasons entirely from the character — and produced geometry a player could
walk and a bot could not path. **The character can climb it and the navmesh will not
generate on it** is a silent failure: no warning, no log line, just bots that never go
up. Anyone editing arena geometry for AI traversal sizes to the agent, and 34uu of agent
radius is why the treads are as deep as they are.

**Where this ticket's own analysis was wrong:** option 1 was costed as "nine steps at
<=45uu rise, 33uu tread inside the existing 3m run". Both numbers were wrong for the
consumer that mattered. The option was still the right call and still the smallest change
— it was just the wrong ruler.

**Left open elsewhere, deliberately:** the fourth Done-when box (AIB9's dz +300..+499
climb-band refusals falling to ~0) is reassigned to AIB9 rather than ticked here. AIB9
owns that instrument, is open, and has never run its five-match sweep. Archiving this
ticket does not bank that number, and nobody should cite these 90 samples as if it did.
