# TICKET AIB28 — Arena01 connectivity: spawn pads, catwalk stairs, gantry/core tops

> STATUS: open — cut by lead (AIB22 W-VERIFY v3) 2026-09-03. Needs the editor (census +
> regen); gated by nothing, but AIB22's Arena01 gates cannot pass until this lands.

The AIB22 census and verification matches measured three MAP defects on `BR_Arena01` that no
bot code can fix (rulings in TICKET_AIB22 Log, 2026-09-03). Blockouts are generated by committed
scripts (law 7) — every fix here is a generator change + regen, never a hand edit.

**Ordering law:** step 1 (measure) gates 2–4; step 5 (regen + census PASS) gates the close.

## Kickoff (machine-checkable — the tickets skill verifies these BEFORE a claim)

- requires: editor-live
- `Tools/aib/aib22_platform_census.py` exists and `python3 -m py_compile` passes
- `Tools/blockout/arena_plan.py` and `Tools/blockout/build_arena.py` exist
- owner_path: Tools/blockout/, Content/Data/, Content/Maps/BR_Arena01.umap, docs/tickets/

## Evidence (2026-09-03, editor-live census over the MCP console; headless v3 matches)

- Floor `BR_Floor_Ground` and all 13 `BR_LM_Mezzanine_Catwalks_0{3,4}_step*` pieces: no full
  path to or from the PlayerStart tier (z 410) in either direction — partial ends 400–530 uu
  short (the storey). Every step's nav top reads at floor height.
- Corner spawn pads (feet z 130, pawn z 218, e.g. (697,697), (3278,3269)): islands — bots
  spawn there, stall "532uu across / −218uu up — link=no", and the island confirmation refutes
  itself because the spawn IS the island (fixed in AIB22 fix #4 by an anchor list, but a
  spawn on an island stays a map defect).
- `BR_LM_The_Gantry_01/02` (z 800, 400x400) and `BR_LM_The_Core_02..06` (z 900): whole-top
  nav with no path either way; bots that grapple up read feet 959–1217 vs nav 910
  (`grounded=no`) — the walkable top sits above its own navmesh.
- RecastNavMesh-Default: generate_nav_links True, BN_Drop (400/1000/50) and BN_Climb
  (250/−70/90) present, DYNAMIC runtime generation — the configs are right; the geometry is not.

## Steps (in order)

1. arena-architect (read-only): from `Content/Data/arena_manifest.json` + `arena_plan.py`,
   state per defect what the generator emits vs what the manifest asked (stair spans, pad
   heights, gantry/core top thickness vs nav agent). One paragraph each, with the numbers.
2. aib-editor: `stair_steps()` in `arena_plan.py` — the flight must RISE (each tread's z climbs
   by ≤ STAIR_MAX_RISE_M from ground to the mezzanine). Reproduce first with the step dump
   (`Tools/aib/aib22_arena_steps.py` in the editor console), then fix, regen, dump again.
3. aib-editor: corner spawn pads get a ramp or step flight to the floor (≤ 35 uu risers) OR the
   PlayerStarts move to the floor/mezzanine — architect's call in step 1.
4. aib-editor: gantry/core tops — either the mesh top is the nav surface (thin walkable slab on
   top of the mass) or the mass is one solid so the nav is on the real top. Then a BN_Drop
   must generate from the gantry edge to the floor (JumpLength 400 — the landing must be within
   400 uu horizontally of the edge and on nav).
5. Regen `BR_Arena01` by script; nav rebuild; `aib22_platform_census.py` on it: `box 2 PASS`
   (every tier reaches the anchor and back); commit the umap + JSON of the census log lines.
6. verifier: 3 headless Arena01 matches at the AIB22 command; idle/stuck medians reported vs
   `Tools/aib/baselines/aib22-arena01-2026-09-02-v2.json`.

## Done when

- [ ] Step dump shows the catwalk treads climbing ground -> mezzanine in ≤ 35 uu risers
- [ ] Census `box 2 PASS` on BR_Arena01 (tiers above AND below the anchor)
- [ ] No spawn pad is an island (each PlayerStart has a full path to the objective)
- [ ] A bot standing on the gantry top reads grounded with its feet on nav (PIE, captured)
- [ ] 3 headless matches: kills/min ≥ v2 baseline median, idle median < v2 baseline
