#pragma once
// PHASE 4 — not yet implemented. Contract:
// A resolved tier: the FAIBTierRow loaded into a queryable vector — Level(EAIBSkill).
// Policies take their level from here; nothing else reads the row directly.
