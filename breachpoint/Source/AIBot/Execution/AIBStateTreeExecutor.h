#pragma once
// PHASE 3 — not yet implemented. Contract:
// IAIBExecutor over UStateTreeAIComponent. One tree (ST_AIBBot); top-level branches gate
// on the active ambition tag; stimulus behaviours are event-driven transitions (the
// Halo 2 lineage). Loads the tree from a soft ini path; a miss is one loud line and a
// standing bot, never a crash.
