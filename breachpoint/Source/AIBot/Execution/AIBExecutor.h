#pragma once
// PHASE 3 — not yet implemented. Contract:
// The seam that keeps StateTree and Behavior Tree interchangeable: Start(controller),
// Stop(), SetActiveAmbition(tag). Pure C++ interface; UAIBStateTreeExecutor first, a BT
// executor may join later with zero brain changes.
