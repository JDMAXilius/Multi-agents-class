#pragma once
// PHASE 3 — not yet implemented. Contract:
// Thin nodes only: every task speaks verbs (avatar door) and facts — no ASC, no game
// types, no decisions (deciding is the brain's). Node structs transcribed from the
// engine's StateTree API at implementation time, never from memory.
