#pragma once
// PHASE 7 — not yet implemented. Contract:
// The claims board (server-only world subsystem): pickups and objective slots are
// claimed; a claimed target scores ~0 for every other bot; claims expire; tier-gated by
// Teamwork competence. Fixes the failure per-agent utility CANNOT express — five correct
// bots all converging on one rocket (Halo shipped this bug; we ship the fix).
