#pragma once
// PHASE 2 — not yet implemented. Contract:
// Halo 1:1 arbitration, worldless: Rescore(Facts, now) scores every registered ambition
// (base x considerations product), applies the incumbent switch-cost bonus, honors commit
// windows, and lets ONLY registered hard interrupts (incoming blast, health cliff) break
// a commit. Returns the winning ambition tag; the executor gates on it. Dithering,
// commit bypass and interrupt starvation are aib-critic named attacks — the spec suite
// pins each one.
