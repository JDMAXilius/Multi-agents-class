#pragma once
// PHASE 2 — not yet implemented. Contract:
// An ambition spec: identity tag (AIBot.Ambition.*), base utility, its considerations,
// commit window seconds, and which hard interrupts may break it. Core set + mode
// contributions (FAIBModeAmbition) normalize into this one shape before scoring.
