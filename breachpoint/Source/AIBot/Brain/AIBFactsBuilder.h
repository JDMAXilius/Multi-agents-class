#pragma once
// PHASE 2 — not yet implemented. Contract:
// The one world-touching file in Brain/: assembles FAIBFacts each think from the avatar
// door (self block), the sensorium (target-as-perceived block), and IAIBWorldQuery +
// IAIBAmbitionProvider (mode block). Everything downstream of it is worldless. A fact a
// human could not have had does not get built (FAIRPLAY F3).
